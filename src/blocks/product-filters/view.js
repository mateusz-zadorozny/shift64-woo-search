/**
 * Product Filters view module.
 *
 * Phase 1 ships the progressive details/summary rendering with no scripted
 * behavior; this store is the Phase 2 attachment point for one-open-at-a-time
 * disclosure, draft/apply selection, the narrow-screen tray, and router
 * navigation (spec: .ai/specs/2026-07-30-product-filter-pill-blocks.md).
 */
import { store } from '@wordpress/interactivity';

store( 'shift64-woo-search/product-filters', {
	state: {},
	actions: {},
} );
